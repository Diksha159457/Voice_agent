# requirements.txt — Lightweight Voice Agent dependencies
# Install: pip install -r requirements.txt
#
# Total download: ~300 MB  (vs 6 GB+ with PyTorch + Ollama)

flask>=3.0.0            # Web server — replaces Streamlit
faster-whisper>=1.0.0   # STT — no PyTorch needed (CTranslate2 backend)
groq>=0.9.0             # Cloud LLM — free key at https://console.groq.com
numpy>=1.24.0           # Required by faster-whisper internally
requests>=2.31.0        # HTTP utility (used by Groq SDK)
python-dotenv>=1.0.0    # Loads GROQ_API_KEY from .env file automatically

# ffmpeg must also be installed on your OS (not via pip):
#   Mac   → brew install ffmpeg
#   Linux → sudo apt install ffmpeg
#   Win   → winget install ffmpeg   OR   choco install ffmpeg