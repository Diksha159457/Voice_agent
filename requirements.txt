# ── Web Framework ──────────────────────────────────────────
streamlit                    # The web UI framework (MISSING from your list — add this!)

# ── Speech to Text ─────────────────────────────────────────
faster-whisper==1.2.1        # Faster version of OpenAI Whisper for transcription
ctranslate2==4.7.1           # Backend engine that faster-whisper depends on
av==17.0.0                   # Audio/video file reading (needed by faster-whisper)
tokenizers==0.22.2           # Tokenizer used by Whisper model

# ── LLM / AI ───────────────────────────────────────────────
groq==1.1.2                  # Groq API client — connects to LLaMA3 in the cloud

# ── HTTP & Networking ───────────────────────────────────────
httpx==0.28.1                # Async HTTP client (used internally by groq)
httpcore==1.0.9              # Low-level HTTP engine under httpx
requests==2.33.1             # Standard HTTP library for simple API calls
urllib3==2.6.3               # HTTP connection pooling (used by requests)
certifi==2026.2.25           # SSL certificates for secure HTTPS connections
charset-normalizer==3.4.7    # Detects and normalizes text encoding in HTTP responses
idna==3.11                   # Internationalized domain names support (used by requests)

# ── Environment & Config ────────────────────────────────────
python-dotenv==1.2.2         # Loads .env file variables (GROQ_API_KEY etc.) locally

# ── Data & ML Utilities ─────────────────────────────────────
numpy==2.4.4                 # Numerical arrays — used for audio data processing
huggingface_hub==1.11.0      # Downloads Whisper model weights from HuggingFace
hf-xet==1.4.3                # Fast file transfer for HuggingFace downloads
fsspec==2026.3.0             # Filesystem abstraction used by huggingface_hub
filelock==3.28.0             # Prevents race conditions when downloading model files
tqdm==4.67.3                 # Progress bars (shown during model download)
packaging==26.1              # Parses version strings (used by many libraries)

# ── AI Model Runtime ────────────────────────────────────────
onnxruntime==1.24.4          # Runs optimized ML models (used by faster-whisper)
flatbuffers==25.12.19        # Serialization format used by onnxruntime
protobuf==7.34.1             # Data serialization (used by onnxruntime/ML models)
sympy==1.14.0                # Symbolic math (dependency of torch/onnx)
mpmath==1.3.0                # Arbitrary precision math (used by sympy)

# ── Pydantic / Type Validation ──────────────────────────────
pydantic==2.13.1             # Data validation and settings management
pydantic_core==2.46.1        # Rust-based core of pydantic (faster validation)
annotated-types==0.7.0       # Type annotation helpers used by pydantic
typing_extensions==4.15.0    # Backports newer Python typing features
typing-inspection==0.4.2     # Runtime inspection of type annotations

# ── Async / Concurrency ─────────────────────────────────────
anyio==4.13.0                # Async I/O compatibility layer (used by httpx/groq)
sniffio==1.3.1               # Detects which async library is running (asyncio/trio)
h11==0.16.0                  # Pure-Python HTTP/1.1 protocol implementation


# ── setuptools ──────────────────────────────────────────────
setuptools==82.0.1           # Python package building tools (safe to keep)